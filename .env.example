# Copy to .env and customize host ports if defaults conflict.
APP_HOST_PORT=8001
DB_HOST_PORT=5433
REDIS_HOST_PORT=6380

# Local redis password used by docker-compose service and app REDIS_URL
REDIS_PASSWORD=ampairedis
